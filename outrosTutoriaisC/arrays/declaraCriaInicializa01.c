/**
 * declara, cria e inicializa um vetor
 * para criar um vetor primeiro define-se 
 * o tipo (int, char, float) (int) num[];
 * segundo especifica-se um nome int (num)[] 
 * terceiro coloca-se cochetes[] a frente
 * do nome int nomeDoArray([]). Para inserir
 * valores usa-se uma lista de valores separado
 * por vírgula entre chaves {valores}
 * int nomeDoArray[] = {1, 2, 3, 4, 5};
 * todos os valores devem ser do meso tipo.
 */
